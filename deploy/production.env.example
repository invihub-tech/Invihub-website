# Cloudflare / production frontend env (Pages or Workers static assets).
# Set in the Cloudflare dashboard — never commit real keys.

VITE_SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"
VITE_ADMIN_PATH_KEY="invitech"
VITE_RAZORPAY_KEY_ID="dummy_key_not_for_live"

# SPA: ensure /shop and nested routes fall back to index.html (see public/_redirects
# and deploy/CLOUDFLARE.md). Do not proxy /api to Express anymore.
