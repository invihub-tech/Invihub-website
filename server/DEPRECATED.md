# Legacy Express/Prisma API

This directory is the **pre-migration** Node/Express API. The React app no longer calls it.

New stack: Supabase Database + Auth + Storage + Edge Functions (`supabase/`).

Keep this folder until the Supabase deployment is verified in a test project; then delete with approval.
